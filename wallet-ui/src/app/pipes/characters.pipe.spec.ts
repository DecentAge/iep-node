import { CharactersPipe } from './characters.pipe';

describe('CharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new CharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
