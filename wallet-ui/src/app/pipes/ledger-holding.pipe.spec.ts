import { LedgerHoldingPipe } from './ledger-holding.pipe';

describe('LedgerHoldingPipe', () => {
  it('create an instance', () => {
    const pipe = new LedgerHoldingPipe();
    expect(pipe).toBeTruthy();
  });
});
