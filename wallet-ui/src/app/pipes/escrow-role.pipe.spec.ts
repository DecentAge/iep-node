import { EscrowRolePipe } from './escrow-role.pipe';

describe('EscrowRolePipe', () => {
  it('create an instance', () => {
    const pipe = new EscrowRolePipe();
    expect(pipe).toBeTruthy();
  });
});
