import { ShufflingStagePipe } from './shuffling-stage.pipe';

describe('ShufflingStagePipe', () => {
  it('create an instance', () => {
    const pipe = new ShufflingStagePipe();
    expect(pipe).toBeTruthy();
  });
});
