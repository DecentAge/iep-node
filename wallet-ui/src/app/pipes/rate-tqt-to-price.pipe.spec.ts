import { RateTqtToPricePipe } from './rate-tqt-to-price.pipe';

describe('RateTqtToPricePipe', () => {
  it('create an instance', () => {
    const pipe = new RateTqtToPricePipe();
    expect(pipe).toBeTruthy();
  });
});
