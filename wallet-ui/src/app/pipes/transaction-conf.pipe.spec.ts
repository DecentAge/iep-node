import { TransactionConfPipe } from './transaction-conf.pipe';

describe('TransactionConfPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionConfPipe();
    expect(pipe).toBeTruthy();
  });
});
