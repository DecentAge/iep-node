import { QuantityQntPipe } from './quantity-qnt.pipe';

describe('QuantityQntPipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityQntPipe();
    expect(pipe).toBeTruthy();
  });
});
