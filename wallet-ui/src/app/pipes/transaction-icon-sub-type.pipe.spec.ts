import { TransactionIconSubTypePipe } from './transaction-icon-sub-type.pipe';

describe('TransactionIconSubTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionIconSubTypePipe();
    expect(pipe).toBeTruthy();
  });
});
