import { PricePerShareBuyOrderPipe } from './price-per-share-buy-order.pipe';

describe('PricePerShareBuyOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new PricePerShareBuyOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
