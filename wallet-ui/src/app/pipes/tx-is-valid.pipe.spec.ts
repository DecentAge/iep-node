import { TxIsValidPipe } from './tx-is-valid.pipe';

describe('TxIsValidPipe', () => {
  it('create an instance', () => {
    const pipe = new TxIsValidPipe();
    expect(pipe).toBeTruthy();
  });
});
