import { SearchTermPipe } from './search-term.pipe';

describe('SearchTermPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTermPipe();
    expect(pipe).toBeTruthy();
  });
});
