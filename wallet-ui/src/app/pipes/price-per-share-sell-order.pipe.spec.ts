import { PricePerShareSellOrderPipe } from './price-per-share-sell-order.pipe';

describe('PricePerShareSellOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new PricePerShareSellOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
