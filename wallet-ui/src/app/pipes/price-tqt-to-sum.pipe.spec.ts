import { PriceTqtToSumPipe } from './price-tqt-to-sum.pipe';

describe('PriceTqtToSumPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceTqtToSumPipe();
    expect(pipe).toBeTruthy();
  });
});
