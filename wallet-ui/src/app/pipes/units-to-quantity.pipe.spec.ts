import { UnitsToQuantityPipe } from './units-to-quantity.pipe';

describe('UnitsToQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitsToQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
