import { QuantityToSharePipe } from './quantity-to-share.pipe';

describe('QuantityToSharePipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityToSharePipe();
    expect(pipe).toBeTruthy();
  });
});
