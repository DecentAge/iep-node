import { ShareToQuantityPipe } from './share-to-quantity.pipe';

describe('ShareToQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new ShareToQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
