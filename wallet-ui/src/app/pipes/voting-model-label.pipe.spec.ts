import { VotingModelLabelPipe } from './voting-model-label.pipe';

describe('VotingModelLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new VotingModelLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
