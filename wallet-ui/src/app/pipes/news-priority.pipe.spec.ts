import { NewsPriorityPipe } from './news-priority.pipe';

describe('NewsPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
