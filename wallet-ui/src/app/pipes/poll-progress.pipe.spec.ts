import { PollProgressPipe } from './poll-progress.pipe';

describe('PollProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new PollProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
