import { AmountToQuantPipe } from './amount-to-quant.pipe';

describe('AmountToQuantPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountToQuantPipe();
    expect(pipe).toBeTruthy();
  });
});
