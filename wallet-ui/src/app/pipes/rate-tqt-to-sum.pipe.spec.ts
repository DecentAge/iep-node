import { RateTqtToSumPipe } from './rate-tqt-to-sum.pipe';

describe('RateTqtToSumPipe', () => {
  it('create an instance', () => {
    const pipe = new RateTqtToSumPipe();
    expect(pipe).toBeTruthy();
  });
});
