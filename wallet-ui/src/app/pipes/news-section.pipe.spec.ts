import { NewsSectionPipe } from './news-section.pipe';

describe('NewsSectionPipe', () => {
  it('create an instance', () => {
    const pipe = new NewsSectionPipe();
    expect(pipe).toBeTruthy();
  });
});
