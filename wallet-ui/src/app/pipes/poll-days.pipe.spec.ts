import {PollDaysPipe} from './poll-days.pipe';

describe('PollDaysPipe', () => {
    it('create an instance', () => {
        const pipe = new PollDaysPipe();
        expect(pipe).toBeTruthy();
    });
});
