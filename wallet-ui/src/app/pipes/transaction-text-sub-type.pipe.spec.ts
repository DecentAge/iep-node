import { TransactionTextSubTypePipe } from './transaction-text-sub-type.pipe';

describe('TransactionTextSubTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionTextSubTypePipe();
    expect(pipe).toBeTruthy();
  });
});
