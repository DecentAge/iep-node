import { HasMessageDirectionPipe } from './has-message-direction.pipe';

describe('HasMessageDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new HasMessageDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
