import { AmountTknPipe } from './amount-tkn.pipe';

describe('AmountTknPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountTknPipe();
    expect(pipe).toBeTruthy();
  });
});
