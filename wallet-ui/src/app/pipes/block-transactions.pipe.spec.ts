import { BlockTransactionsPipe } from './block-transactions.pipe';

describe('BlockTransactionsPipe', () => {
  it('create an instance', () => {
    const pipe = new BlockTransactionsPipe();
    expect(pipe).toBeTruthy();
  });
});
