import { LessorsPercentagePipe } from './lessors-percentage.pipe';

describe('LessorsPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new LessorsPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
