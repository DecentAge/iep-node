import { AmountTqtPipe } from './amount-tqt.pipe';

describe('AmountTqtPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountTqtPipe();
    expect(pipe).toBeTruthy();
  });
});
