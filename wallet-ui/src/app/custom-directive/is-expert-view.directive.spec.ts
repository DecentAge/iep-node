import { IsExpertViewDirective } from './is-expert-view.directive';

describe('IsExpertViewDirective', () => {
  it('should create an instance', () => {
    const directive = new IsExpertViewDirective();
    expect(directive).toBeTruthy();
  });
});
