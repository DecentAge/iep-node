import { IsAppEnabledDirective } from './is-app-enabled.directive';

describe('IsAppEnabledDirective', () => {
  it('should create an instance', () => {
    const directive = new IsAppEnabledDirective();
    expect(directive).toBeTruthy();
  });
});
