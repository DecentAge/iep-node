import { AutoRefreshDirective } from './auto-refresh.directive';

describe('AutoRefreshDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoRefreshDirective();
    expect(directive).toBeTruthy();
  });
});
